services:
  web:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/kanbanflow.db
      FORWARDED_ALLOW_IPS: "*"
    volumes:
      - ./data:/data
